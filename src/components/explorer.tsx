'use client';
import {useMemo,useState,useCallback} from 'react';
import Link from 'next/link';
import {useRouter} from 'next/navigation';
import {Search,ArrowUpRight} from 'lucide-react';
import {ResponsiveContainer,ScatterChart,Scatter,XAxis,YAxis,CartesianGrid,Tooltip,ReferenceLine,ZAxis} from 'recharts';
import {data,fmt,departments,groups,alphaText,profURL,Professor,Metrics} from '@/lib/model';
import {usePeriod} from './shell';
import {useExplorerTools} from './use-explorer-tools';
export type Point=Professor & Metrics;
export function ProfessorScatter({points,search=''}:{points:Point[];search?:string}){const router=useRouter();const {period}=usePeriod();const plot=points.map(p=>({...p,highlight:!!search&&p.name.toLowerCase().includes(search.toLowerCase())}));const matches=plot.filter(p=>p.highlight);const selected=(v:unknown)=>{const p=v as Point;if(p?.id)router.push(`/professors/${p.id}/`)};return <><div className="scatter-wrap"><div className="quadrant q1">Easy + Loved</div><div className="quadrant q2">Hard + Loved</div><div className="quadrant q3">Easy + Disliked</div><div className="quadrant q4">Hard + Disliked</div><ResponsiveContainer width="100%" height="100%" minWidth={0}><ScatterChart margin={{top:30,right:22,bottom:36,left:2}}><CartesianGrid stroke="#e8e7ed" strokeDasharray="2 4"/><XAxis type="number" dataKey="difficulty" domain={[1,5]} ticks={[1,2,3,4,5]} name="Average difficulty" tickLine={false} axisLine={false} label={{value:'Average difficulty →',position:'bottom',offset:15}}/><YAxis type="number" dataKey="quality" domain={[1,5]} ticks={[1,2,3,4,5]} name="Average quality" width={48} tickLine={false} axisLine={false} label={{value:'Average quality →',angle:-90,position:'insideLeft',offset:8}}/><ZAxis type="number" dataKey="reviews" range={[45,230]} domain={[1,200]}/><ReferenceLine x={data.means[period].difficulty} stroke="#aaaab9" strokeDasharray="5 5"/><ReferenceLine y={data.means[period].quality} stroke="#aaaab9" strokeDasharray="5 5"/><Tooltip cursor={{strokeDasharray:'3 3'}} content={({active,payload})=>{if(!active||!payload?.length)return null;const p=payload[0].payload as Point;return <div className="chart-tooltip"><strong>{p.name}</strong><span>{p.department} · {fmt(p.reviews,0)} reviews</span><dl><dt>Quality</dt><dd>{fmt(p.quality)}</dd><dt>Difficulty</dt><dd>{fmt(p.difficulty)}</dd><dt>Bayesian quality</dt><dd>{fmt(p.bayesianQuality)}</dd><dt>Professor Alpha</dt><dd>{fmt(p.alpha)}</dd></dl><small>Click to see professor & reviews ↗</small></div>}}/><Scatter name="Professors" data={plot.filter(p=>!p.highlight)} fill="#000066" fillOpacity={search?.28:.65} stroke="#faf9f7" strokeWidth={1} onClick={selected} style={{cursor:'pointer'}} isAnimationActive={false}/>{matches.length>0&&<Scatter name="Search matches" data={matches} fill="#990000" stroke="#660000" strokeWidth={2} onClick={selected} style={{cursor:'pointer'}} isAnimationActive={false}/>}</ScatterChart></ResponsiveContainer></div><div className="chart-caption"><span><i className="dot"/> One dot = one professor · Size = review count</span><span>Dashed lines = Richmond review averages</span></div>{matches.length>0&&<div className="search-matches">Highlighted: {matches.slice(0,8).map(p=><Link key={p.id} href={profURL(p)}>{p.name} ↗</Link>)}</div>}<p className="micro">Both rating scales run from 1 to 5. Hover for details; click a dot to open reviews. <span title={alphaText}>Alpha is a custom, difficulty-, department-, and year-adjusted rating measure.</span></p>{points.length===0&&<p className="empty">No professors match these filters. Try fewer restrictions.</p>}</>}
const sortOptions=[['best','Best'],['worst','Worst'],['hardest','Hardest'],['easiest','Easiest'],['reviews','Most reviews'],['alpha','Highest Alpha'],['lowAlpha','Lowest Alpha'],['polarization','Most polarizing']];
export function ProfessorTable({points,sort='best'}:{points:Point[];sort?:string}){const [limit,setLimit]=useState(40);const field=sort==='hardest'||sort==='easiest'?'bayesianDifficulty':sort==='reviews'?'reviews':sort==='alpha'||sort==='lowAlpha'?'alpha':sort==='polarization'?'polarization':'bayesianQuality';const asc=['worst','easiest','lowAlpha'].includes(sort);const sorted=[...points].sort((a,b)=>{const x=a[field],y=b[field];return x==null?y==null?a.name.localeCompare(b.name):1:y==null?-1:(asc?x-y:y-x)||a.name.localeCompare(b.name)});return <><div className="table-scroll"><table><thead><tr><th>Professor</th><th>Quality</th><th>Difficulty</th><th>Reviews</th><th title={alphaText}>Alpha ⓘ</th><th>Adjusted quality</th></tr></thead><tbody>{sorted.slice(0,limit).map(p=><tr key={p.id}><td><Link className="prof-link" href={profURL(p)}>{p.name}<ArrowUpRight size={15}/></Link><span className="subcell">{p.department}</span></td><td><span className={`rating ${p.quality<3?'low':''}`}>{fmt(p.quality)}</span></td><td>{fmt(p.difficulty)}</td><td>{fmt(p.reviews,0)}</td><td className={p.alpha!=null&&p.alpha>0?'positive':''}>{p.alpha!=null&&p.alpha>0?'+':''}{fmt(p.alpha)}</td><td>{fmt(p.bayesianQuality)}</td></tr>)}</tbody></table></div>{sorted.length===0&&<p className="empty">No professors match. Try clearing a filter.</p>}{sorted.length>limit&&<button className="button" onClick={()=>setLimit(limit+40)}>Show 40 more</button>}<p className="micro">Best / worst and hardest / easiest use Bayesian scores where available. Missing scores appear last. Alpha is custom; positive means higher ratings than expected for difficulty, department, and year.</p></>}
export function Explorer({fixedDepartment,table=false}:{fixedDepartment?:string;table?:boolean}){const {period,setPeriod}=usePeriod();const [search,setSearch]=useState('');const [department,setDepartment]=useState(fixedDepartment||'');const [group,setGroup]=useState('');const [minOverride,setMin]=useState<number|null>(null);const min=minOverride??(period==='all'?20:15);const [sort,setSort]=useState('best');const applyFilters=useCallback((v:{search:string;department:string;group:string;minimumReviews:number;period:'all'|'recent'})=>{setSearch(v.search);setDepartment(v.department);setGroup(v.group);setMin(v.minimumReviews);setPeriod(v.period)},[setPeriod]);useExplorerTools(applyFilters,fixedDepartment);const points=useMemo(()=>data.professors.flatMap(p=>{const m=p[period];return m&&m.reviews>=min&&(!department||p.department===department)&&(!group||p.group===group)?[{...p,...m}]:[]}),[period,min,department,group]);const filtered=table?points.filter(p=>p.name.toLowerCase().includes(search.toLowerCase())):points;return <div className="explorer"><div className="filters"><label className="search-field"><Search size={18}/><input aria-label="Search professors" placeholder="Find your professor…" value={search} onChange={e=>setSearch(e.target.value)}/></label>{!fixedDepartment&&<label><span>Department</span><select aria-label="Department" value={department} onChange={e=>setDepartment(e.target.value)}><option value="">All departments</option>{departments.map(d=><option key={d}>{d}</option>)}</select></label>}<label><span>Academic group</span><select aria-label="Academic group" value={group} onChange={e=>setGroup(e.target.value)}><option value="">All academic groups</option>{groups.map(g=><option key={g}>{g}</option>)}</select></label><label><span>Minimum reviews</span><select aria-label="Minimum reviews" value={min} onChange={e=>setMin(+e.target.value)}>{[1,5,15,20,30,50,100].map(n=><option key={n} value={n}>{n}+ reviews</option>)}</select></label>{table&&<label><span>Sort by</span><select aria-label="Sort professors" value={sort} onChange={e=>setSort(e.target.value)}>{sortOptions.map(([v,l])=><option key={v} value={v}>{l}</option>)}</select></label>}</div><div className="results-meta"><span>{fmt(filtered.length,0)} professors · {period==='all'?'All time':'2022–2026'}</span><button onClick={()=>{setSearch('');setDepartment(fixedDepartment||'');setGroup('');setMin(null);setSort('best')}}>Reset filters ↺</button></div>{table?<ProfessorTable points={filtered} sort={sort}/>:<ProfessorScatter points={points} search={search}/>}</div>}
